# ============================================================================
# Universal Web Template - 环境变量配置模板
# ============================================================================
#
# 使用说明：
# 1. 复制此文件为 backend/.env
# 2. 修改所有标记为【必须修改】的值
# 3. 其他值根据需要调整
#
# ⚠️  重要：不要将 .env 文件提交到 Git！
# ============================================================================

# ============================================================================
# 站点配置【必须修改】
# ============================================================================

# 站点ID（site1, site2, ..., site20）
SITE_ID=siteN

# Node.js环境
NODE_ENV=production

# 服务端口（Docker内部，通常不需要修改）
PORT=3000

# 时区（IANA格式）
TZ=America/New_York

# ============================================================================
# PostgreSQL总系统配置【必须修改】
# ============================================================================

# 数据库主机（使用Docker网络别名，不要修改）
DB_HOST=postgres_master

# 数据库端口（通常不需要修改）
DB_PORT=5432

# 数据库名称（使用主数据库，不要修改）
DB_NAME=postgres

# 站点数据库用户（必须与站点ID对应）
DB_USER=siteN_user

# 站点数据库密码【必须修改为强密码】
DB_PASS=siteN_pass

# SSL配置（VPS环境不使用SSL）
DB_SSL=false

# ============================================================================
# JWT认证配置【必须修改】
# ============================================================================

# JWT密钥【必须修改为随机字符串】
JWT_SECRET=change-this-to-a-random-secret-key

# JWT过期时间
JWT_EXPIRES_IN=15m

# 刷新令牌过期时间
JWT_REFRESH_EXPIRES_IN=7d

# ============================================================================
# CORS配置【必须修改】
# ============================================================================

# 前端URL（主域名）
FRONTEND_URL=https://example.com

# 允许的CORS源（逗号分隔）
ALLOWED_ORIGINS=https://example.com,https://www.example.com

# ============================================================================
# 反馈系统配置【必须修改】
# ============================================================================

# 反馈系统站点ID
FEEDBACK_SITE_ID=siteN

# ============================================================================
# 缓存配置（可选）
# ============================================================================

# 启用缓存
CACHE_ENABLED=true

# 缓存TTL（秒）
CACHE_TTL=300

